#pragma once
#include <vector>

class AliasMethodSampling
{
    public:

        float sample(float xi, float& p)
        {
            return 0.f;
        }

    private:
        std::vector<float> m_vec;
};